name: V19 Financial Lock Key Final

on:
  push:
    branches:
      - v19-production-readiness
    paths:
      - 'tools/v19_fix_financial_lock_key.py'
      - '.github/workflows/v19-financial-lock-key-final.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  financial-core:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: zhirox
          POSTGRES_PASSWORD: zhirox_test_password
          POSTGRES_DB: zhirox_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U zhirox -d zhirox_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://zhirox:zhirox_test_password@127.0.0.1:5432/zhirox_test
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v19-production-readiness
      - name: Generate final hardened financial core
        run: python3 tools/v19_fix_financial_lock_key.py
      - name: Syntax-check
        working-directory: source/server
        run: node --check src/app.mjs
      - name: Install and audit server
        working-directory: source/server
        run: |
          npm ci
          npm audit --omit=dev --audit-level=high
      - name: Run all PostgreSQL financial race tests
        working-directory: source/server
        run: npm test
      - name: Revalidate frontend production gates
        working-directory: source
        run: |
          npm ci
          npm run check:production
          npx tsc --noEmit
          npm run build
      - name: Commit verified financial authority
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add source/server
          if git diff --cached --quiet; then
            echo 'Financial authority already committed.'
            exit 0
          fi
          git commit -m 'Add concurrency-safe financial sale authority for v19'
          git push origin HEAD:v19-production-readiness
